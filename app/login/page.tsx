import { Layout } from '../../components/Layout';
import { Login } from '../../views/Login';

export default function LoginPage() {
  return (
    <Layout hideNav>
      <Login />
    </Layout>
  );
}
